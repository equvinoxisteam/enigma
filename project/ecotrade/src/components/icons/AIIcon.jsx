import { BrainCircuit } from 'lucide-react';

/** Consistent AI feature icon (replaces sparkle/star styling). */
export default function AIIcon({ size = 18, className = '' }) {
  return <BrainCircuit size={size} className={className} />;
}
